using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArucoSignal : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Kreoss Arrived!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
